necesito un jupyter hub que permita inicar un jupyter notebook aislado por usuario utilizando el api de Jupyter hub
Se debe crear una instancia de jupyter notebook por usuario y esta instancia debe ser efimera
Crea el request en postman
modifica el docker compose para que cuando se lance el request mediante postman se carguen las carpetas, el dataset y el notebook que ya se esta cargando por defecto en el docker que esta hasta este momento configurado
